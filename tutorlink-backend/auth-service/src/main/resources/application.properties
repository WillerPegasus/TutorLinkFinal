# NOM ET PORT DU SERVICE
spring.application.name=auth-service
server.port=8081

# BASE DE DONNÉES MYSQL
spring.datasource.url=jdbc:mysql://localhost:3306/tutorlink_auth?createDatabaseIfNotExist=true&useSSL=false&serverTimezone=Africa/Douala
spring.datasource.username=root
spring.datasource.password=ab1234

spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

# EUREKA
eureka.client.service-url.defaultZone=http://localhost:8080/eureka/
eureka.client.register-with-eureka=true
eureka.client.fetch-registry=true
eureka.instance.instance-id=${spring.application.name}:${server.port}

# JWT
jwt.secret=tutorlink_secret_key_2026_very_long_secure
jwt.expiration=86400000

# EMAIL GMAIL
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=abakarkhatiratim@gmail.com
spring.mail.password=wdtd wvjq ssfj gnva
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true

# FEIGN
feign.client.config.default.loggerLevel=FULL
feign.circuitbreaker.enabled=true
spring.datasource.hikari.maximum-pool-size=3
spring.datasource.hikari.initialization-fail-timeout=60000
spring.datasource.hikari.connection-timeout=45000

# DÉSACTIVER Eureka pendant les tests locaux
# Sans ça, le service plante car il ne trouve pas le serveur Eureka
# eureka.client.enabled=false

# DÉSACTIVER le circuit breaker Feign pendant les tests
# Sans ça, Spring cherche notification-service et user-service
# spring.cloud.circuitbreaker.enabled=false

